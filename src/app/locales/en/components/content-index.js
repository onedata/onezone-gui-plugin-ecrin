import queryParameters from './query-parameters';
import _ from 'lodash';

export default _.assign({}, queryParameters, {
  title: 'The Clinical Studies Data Objects Index',
  subtitle: 'Discover the datasets &amp; documents',
  find: 'Find',
});
